
import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const SupportBoard: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Support Board</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Support Board content will appear here.</p>
      </CardContent>
    </Card>
  );
};
